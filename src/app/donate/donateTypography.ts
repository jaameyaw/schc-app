/** Payment detail fields (MoMo + bank copy cards) */

const sectionFont = "font-section";

export const paymentFieldLabel = `${sectionFont} text-[0.68rem] font-semibold uppercase tracking-[0.14em] text-gray-500`;

export const paymentFieldValue = `${sectionFont} text-[0.98rem] font-semibold leading-[1.35] tracking-tight text-pretty text-dark-text sm:text-[1.05rem]`;

export const paymentFieldValueMono = `${paymentFieldValue} font-mono text-[1.02rem] font-medium tabular-nums tracking-[0.04em] sm:text-[1.08rem]`;

export const paymentCopyAction =
  "font-section text-[0.72rem] font-semibold uppercase tracking-[0.08em]";
